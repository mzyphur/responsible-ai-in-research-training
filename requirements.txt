# Python dependencies for building the report and rendering the charts.
# Patch and minor updates are allowed within a major version (the `<X.0`
# upper bound stops a breaking major release from auto-installing).
#
# To regenerate this file from the current environment:
#   pip freeze | grep -iE '^(matplotlib|numpy|pandas|markdown|python-docx|weasyprint|PyYAML|pypdf|Pillow)=='
#
# Non-Python system dependencies:
# - pandoc
# - LibreOffice `soffice` on PATH. The DOCX build depends on the LibreOffice
#   headless round-trip, because Word-for-Mac compatibility relies on the
#   normalised OOXML it emits. On macOS: brew install --cask libreoffice
matplotlib>=3.10,<4.0
numpy>=2.3,<3.0
pandas>=2.3,<3.0
Markdown>=3.10,<4.0
python-docx>=1.2,<2.0
weasyprint>=68.0,<69.0
PyYAML>=6.0,<7.0
pypdf>=6.0,<7.0
Pillow>=12.0,<13.0
